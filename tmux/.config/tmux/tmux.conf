####################### PLUGINS ###############################
set-environment -g TMUX_PLUGIN_MANAGER_PATH "$HOME/.local/share/tmux/plugins"

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'omerxx/tmux-floax'
# set -g @plugin 'joshmedeski/tmux-nerd-font-window-name'
set -g @plugin 'tmux-plugins/tmux-cpu'

####################### OPTIONS ###############################
set -sg escape-time 10


# Set true color
# set-option -sa terminal-overrides ",xterm*:Tc"
# set-option -sa terminal-features ',xterm-kitty:RGB'

set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"

set -g allow-passthrough on
set -ga update-environment TERM
set -ga update-environment TERM_PROGRAM

set -g base-index 1
set -g pane-base-index 1
set -g renumber-windows on
set -g mouse on
# don't rename windows automatically
set-option -g allow-rename off

####################### THEME #################################
# ===== Catppuccin Macchiato palette =====
# background: #24273A
# foreground: #CAD3F5
# black:      #494D64
# brightBlack:#5B6078
# red:        #ED8796
# green:      #A6DA95
# yellow:     #EED49F
# blue:       #8AADF4
# purple:     #F5BDE6
# cyan:       #8BD5CA
# white:      #B8C0E0
# brightWhite:#A5ADCB

set -g status-position top
set -g status-justify left
set -g focus-events on
set -g status-left-length 99
set -g status-right-length 99
set -g status-style bg=default,fg=default

# set -g status-left "#{?client_prefix,#[fg=#ed8796]  #[bold]#S#[fg=#6c7086] ⟩,\
# #[fg=#a6da95]  #[bold]#S#[fg=#6c7086] ⟩} #[fg=#a6adc8]#I:#W #[fg=#8BD5CA]#{pane_current_command}#[fg=#6c7086] ⟩ \
# #[fg=#f5e0dc] "

set -g status-left "#{?client_prefix,#[fg=#ed8796]  #[bold]#S#[fg=#6c7086] ⟩,\
#[fg=#a6da95]  #[bold]#S#[fg=#6c7086] ⟩} #[fg=#a6adc8]#I:#[fg=#8BD5CA]#{=4;p4:pane_current_command}#[fg=#6c7086] ⟩\
#[fg=#f5e0dc]  "


set -g status-right "#[fg=#EED49F,bold]󰃭 %Y-%m-%d #[fg=#5B6078]⟨ #[fg=#F4DBD6,bold]󰥔 %H:%M #[fg=#5B6078]⟨ #[fg=#8AADF4,bold]󰒋 #H #[default]"

set -g copy-mode-match-style 'fg=#1e1e2e,bg=#cdd6f4'
set -g copy-mode-current-match-style 'fg=#1e1e2e,bg=#ed8796'

set -g pane-border-style 'fg=#45475a'
set -g pane-active-border-style 'fg=#f9e2af'

set -g window-status-style 'fg=#585b70'
set -g window-status-current-style 'fg=#f9e2af'

set -g window-status-format "#[fg=#6c7086] #[bold]#{window_index} "
set -g window-status-current-format "#[fg=#f9e2af,bold]|#{window_index}#[default] "



####################### KEYMAPS #################################
unbind-key C-b
set -g prefix C-a

# Reload config
bind-key r source-file ~/.config/tmux/tmux.conf \; \
display-message "Config Reloaded."

# window
bind-key enter new-window

# switch focus pane
set -g @vim_navigator_mapping_left "C-h"
set -g @vim_navigator_mapping_right "C-l"
set -g @vim_navigator_mapping_up "C-k"
set -g @vim_navigator_mapping_down "C-j"
set -g @vim_navigator_mapping_prev ""

# split panes using | and -
unbind-key '"'
unbind-key %
bind-key '\' split-window -h
bind-key - split-window -v
bind-key o rotate-window
bind-key q kill-pane

bind-key i run-shell " \
if [ $(( \$(tmux display -p '8*#{pane_width}-20*#{pane_height}') )) -lt 0 ]; then \
  tmux splitw -v -c '#{pane_current_path}'; \
else \
  tmux splitw -h -c '#{pane_current_path}'; \
fi"

bind-key -n S-Up    resize-pane -U 5
bind-key -n S-Down  resize-pane -D 5
bind-key -n S-Left  resize-pane -L 5
bind-key -n S-Right resize-pane -R 5

# Move window
bind-key -n M-S-Left swap-window -t -1\; select-window -t -1
bind-key -n M-S-Right swap-window -t +1\; select-window -t +1

# visual mode
set-window-option -g mode-keys vi
bind-key c copy-mode
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle \; send-keys -X begin-selection
bind-key -T copy-mode-vi C-u send-keys -X halfpage-up
bind-key -T copy-mode-vi C-d send-keys -X halfpage-down
bind-key -T copy-mode-vi Escape send-keys -X cancel

# floax-bind
# set -g @floax-bind '-n M-p'
# unbind-key f

####################### Initialize #################################
# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.local/share/tmux/plugins/tpm/tpm
